<!-- 这段是关于搜索功能，但是外观形态还未完成 -->
<form>
  <div class="form-group">
    <%= form_tag student_ck_path, :method => :get do %>
      站段名称: <%= text_field_tag :registration_id, params[:registration_id], :placeholder => "站段搜索" %>
    <%= submit_tag "送出筛选", :class => "btn btn-primary" %>
     <% end %>
   </div>
</form>
  <div class="dropdown clearfix pull-right">
    <button class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenuDivider" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
      考试状态
      <span class="caret"></span>
    </button>
    <!-- <% if params[:search].present? %>
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuDivider">
      <li>
        <%= link_to("未考", student_ck_path(:order => "student_wk")) %>
      </li>
      <li>
        <%= link_to("已考", student_ck_path(:order => "student_ck"))%>
      </li>
    </ul>
    <% else %> -->
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuDivider">
      <li>
        <%= link_to("未考", student_ck_path(:order => "by_student_wk")) %>
      </li>
      <li>
        <%= link_to("已考", student_ck_path(:order => "by_student_ck"))%>
      </li>
    </ul>
    <!-- <% end %> -->
  </div>

  <table class="table table-hover">
      <thead>
        <tr>
          <td>姓名</td>
          <td>部门</td>
          <td>职务</td>
          <td>最近一次考试时间</td>
          <td>考试状态</td>
        </tr>
      </thead>
      <tbody>
          <% @users.each do |user| %>
              <tr>
                <td>
                  <%= link_to(user.F_name, '#')%>
                </td>
                <td>
                  <%= user.t_duan_info.F_name %> /
                  <%= user.t_station_info.F_name %> /
                  <%= user.t_team_info.F_name %>
                </td>
                <td>
                  <%= user.t_work_info.F_name %>
                </td>
                  <% if user.t_record_infoes.present? %>
                  <td>
                    <%= user.t_record_infoes.order("F_time DESC").pluck(:F_time).first %>
                  </td>
                  <td>
                      <p style=" background-color: #d9534f"> <%= "已考" %> </p>
                  </td>
                  <% else %>
                  <td></td>
                  <td>
                      <p style=" background-color: #56b83d"> <%= "未考" %> </p>
                  </td>
                  <% end %>
              </tr>
          <% end %>
      </tbody>
  </table>

  <div class="pull-right">
      <%= paginate @users %>
  </div>
  <script>
    $("#event_category_id").select2( { theme: "bootstrap"} );
  </script>
