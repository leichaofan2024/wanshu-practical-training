<div class="call-board-frame">
  <h3>发布新消息：</h3>
  <%= simple_form_for @call_board do |f| %>
    <%= f.input :name %>
    <%= f.input :content %>
    <%= f.submit "提交",:data => {disable_with: "提交中。。。"}%>
  <% end %>
</div>
