
<div class= "pie_duan_frame">
  <div class="clear">
    <a href="/t_program_infoes/<%= params[:id]%>?<%= request.original_url.split("?")[1] %>">专项考核首页</a> > 各站段专项考试成绩
  </div>
  <div class="row">
    <div class="pull-right range-query">
      <div class="searchtime">时间：</div>
      <div class="searchtime">
        <%= form_tag program_duan_score_info_t_program_info_path(@t_program_info), method: :get do %>
          <%= text_field_tag 'search[date_from]',nil,:placeholder => time_begin(params[:search])%>
          <%= text_field_tag 'search[date_to]',nil,:placeholder => time_end(params[:search]) %>
          <%= hidden_field_tag :name, params[:name]%>
          <%= submit_tag "搜索", id:"time_id",class: "btn search-button"%>
        <% end %>
      </div>
    </div>
  </div>
  <script src="/assets/echarts.min.js"></script>
    <div id="main" style="width: 1000px;height:500px;margin:40px auto 0;"></div>
     <script type="text/javascript">
      // 基于准备好的dom，初始化echarts实例
      var myChart = echarts.init(document.getElementById('main'));

      // 指定图表的配置项和数据
      var option = {
              title: {
                  text: '车务段考试成绩分析'
              },
              tooltip : {
                  trigger: 'axis',
                  axisPointer : {            // 坐标轴指示器，坐标轴触发有效
                      type : 'shadow'        // 默认为直线，可选为：'line' | 'shadow'
                  }
              },
              legend: {
                  data: ['90分以上', '80-90分','60-80分','60分以下']
              },
              grid: {
                  left: '3%',
                  right: '4%',
                  y2:100,
                  containLabel: true
              },
              yAxis:  {
                  type: 'value'
              },
              xAxis: {
                  type: 'category',
                  splitLine: {
                    show: false
                },
                axisTick: {
                    show: false
                },
                splitArea: {
                    show: false
                },
                axisLabel: {
                    interval: 0,
                    rotate: 45,
                    show: true,
                    splitNumber: 15,
                    textStyle: {
                        fontFamily: "微软雅黑",
                        fontSize: 14
                    }
                  },
                  data: gon.duan_key
              },
              series: [
                  {
                      name: '90分以上',
                      type: 'bar',
                      stack: '总分',
                      label: {
                          normal: {
                              show: true,
                              position: 'insideRight'
                          }
                      },
                      data: gon.ninefen
                  },
                  {
                      name: '80-90分',
                      type: 'bar',
                      stack: '总分',
                      label: {
                          normal: {
                              show: true,
                              position: 'insideRight'
                          }
                      },
                      data: gon.ef
                  },
                  {
                      name: '60-80分',
                      type: 'bar',
                      stack: '总分',
                      label: {
                          normal: {
                              show: true,
                              position: 'insideRight'
                          }
                      },
                      data: gon.sf
                  },
                  {
                      name: '60分以下',
                      type: 'bar',
                      stack: '总分',
                      barWidth : 60,
                      label: {
                          normal: {
                              show: true,
                              position: 'insideRight'
                          }
                      },
                      data: gon.sb
                  }
              ]
          };


              // 使用刚指定的配置项和数据显示图表。
        myChart.setOption(option);
        function GetUrlPara(){
            var url = document.location.toString();
            var arrUrl = url.split("?");
            var para = arrUrl[1];
            return para
        };
        myChart.on('click', function(params){
          window.location.assign('/t_program_infoes/'+gon.program_id+'/program_station_score_info?duan_name=' + params.name +'&'+ GetUrlPara() );
        });
  </script>

</div>


<div id="zhishu" style="width: 1000px;height:500px;margin:40px auto 0;"></div>
 <script type="text/javascript">
  // 基于准备好的dom，初始化echarts实例
  var myChart = echarts.init(document.getElementById('zhishu'));

  // 指定图表的配置项和数据
  var option = {
          title: {
              text: '直属站考试成绩分析'
          },
          tooltip : {
              trigger: 'axis',
              axisPointer : {            // 坐标轴指示器，坐标轴触发有效
                  type : 'shadow'        // 默认为直线，可选为：'line' | 'shadow'
              }
          },
          legend: {
              data: ['90分以上', '80-90分','60-80分','60分以下']
          },
          grid: {
              left: '3%',
              right: '4%',
              y2:100,
              containLabel: true
          },
          yAxis:  {
              type: 'value'
          },
          xAxis: {
              type: 'category',
              splitLine: {
                show: false
            },
            axisTick: {
                show: false
            },
            splitArea: {
                show: false
            },
            axisLabel: {
                interval: 0,
                rotate: 45,
                show: true,
                splitNumber: 15,
                textStyle: {
                    fontFamily: "微软雅黑",
                    fontSize: 14
                }
              },
              data: gon.zsduan_key
          },
          series: [
              {
                  name: '90分以上',
                  type: 'bar',
                  stack: '总分',
                  label: {
                      normal: {
                          show: true,
                          position: 'insideRight'
                      }
                  },
                  data: gon.zsnf
              },
              {
                  name: '80-90分',
                  type: 'bar',
                  stack: '总分',
                  label: {
                      normal: {
                          show: true,
                          position: 'insideRight'
                      }
                  },
                  data: gon.zsef
              },
              {
                  name: '60-80分',
                  type: 'bar',
                  stack: '总分',
                  label: {
                      normal: {
                          show: true,
                          position: 'insideRight'
                      }
                  },
                  data: gon.zssf
              },
              {
                  name: '60分以下',
                  type: 'bar',
                  stack: '总分',
                  barWidth : 60,
                  label: {
                      normal: {
                          show: true,
                          position: 'insideRight'
                      }
                  },
                  data: gon.zssb
              }
          ]
      };

          // 使用刚指定的配置项和数据显示图表。
    myChart.setOption(option);
    function GetUrlPara(){
         var url = document.location.toString();
         var arrUrl = url.split("?");
         var para = arrUrl[1];
         return para;
         };
   myChart.on('click', function (params) {
    window.location.assign('/t_program_infoes/'+gon.program_id+'/program_station_score_info?duan_name=' + params.name +'&'+ GetUrlPara());
   });

</script>
</div>
